---
// This route was a zero-byte file and rendered blank in production.
// The product's Register row (price, Beacons URL, affiliate status) was not
// available at time of repair, and inventing one is not permitted — so the
// route redirects to the shop rather than serving an empty page.
// Restore the full bridge page once the Register row is confirmed.
return Astro.redirect("/shop", 302);
---
