---
import PageLayout from "../../layouts/PageLayout.astro";
import { MODULES } from "../../we-manifest.js";
import { S, HREF } from "../../we-stage.js";

const phases = [...new Set(MODULES.map((m) => m.phase))].map((p) => {
  const inPhase = MODULES.filter((m) => m.phase === p);
  const [when, what] = [inPhase[0].phaseLabel, inPhase[0].phaseSub];
  return {
    day: when.split("·")[1]?.trim() ?? when,
    title: what.split("—")[0].trim(),
    sub: what.split("—")[1]?.trim(),
    accent: inPhase[0].accent,
    mods: inPhase,
  };
});

const problems = [
  { t: "It's all trapped in Skool", p: "Every lesson, every call, every post you've written. None of it searchable, none of it ranking, none of it on a domain you own." },
  { t: "Nobody finds you unless they already know you", p: "A member has to join before they can read a word. There's no front door — only a members' entrance." },
  { t: "You post, then you post again", p: "A feed resets to zero every day. Nothing you made last March is working for you this March." },
  { t: "You've started a blog three times", p: "Blank page, no idea what to write, quit by week three. That's not a discipline problem. It's a wrong-starting-point problem." },
];

const ACC = ["#CBB7F7","#CD9CDD","#CF80C2","#D265A8","#D4498D","#D62E73"];

const stats = [
  { n: "22", l: "Modules" },
  { n: "36", l: "Prompts, verbatim" },
  { n: "3", l: "Reference vaults" },
  { n: "1", l: "Weekend" },
];

const included = [
  { t: "22 modules", p: "Each one teaches the step, hands you the prompt, and tells you what breaks there and how to fix it." },
  { t: "The Quick Sheet", p: "All 36 prompts on one page in running order, one line each. For your second build, and your third." },
  { t: "The Prompt Vault", p: "Every prompt verbatim and organised by tool — Claude, Chrome, image generation, GitHub." },
  { t: "The Troubleshooting Vault", p: "An FAQ plus every roadblock from a real build, paired with the fix that worked." },
  { t: "The Template Vault", p: "Canon File, pillar map, live-facts block, product register, operating cadence. Fill in and keep." },
  { t: "Progress tracking + certificate", p: "Tick off each module, watch the ring fill, and unlock a completion certificate at twenty-two." },
  { t: "Every future update, free", p: "This isn't a PDF you download once. It's a live site you log into. When the tools change or I find a better way, new and rewritten modules appear in your account. One payment, forever." },
];

const proof = [
  { t: "How to Build a Faceless Digital Business", u: "/learn/how-to-build-a-faceless-digital-business", n: "3,000+ words" },
  { t: "The Complete Pinterest Marketing Guide", u: "/learn/complete-pinterest-marketing-guide", n: "Pillar + cluster" },
  { t: "Automating Your Online Business", u: "/learn/automating-your-online-business", n: "Built from a lesson" },
];

const claudeDoes = [
  "Writes every file your website needs — config, layouts, pages, the homepage",
  "Turns a Skool lesson into a 3,000-word article written for a stranger",
  "Runs four separate SEO passes over it — Google, Pinterest, AI search, readability",
  "Generates the supporting article cluster around each pillar",
  "Builds your email capture, resource pages and bridge pages",
  "Writes your lead magnets from your own material",
  "Turns one article into ten pins, an 8-slide carousel, eight Threads posts, two reel prompts with matching cover images, a Facebook post, an email and a community post",
  "Generates every image, on brand, without your face",
  "Commits the finished files straight to your site",
];

const youDo = [
  "Paste the prompt",
  "Read what came back",
  "Say yes, or say what to change",
];

const neverDo = [
  "Write code",
  "Design a page",
  "Hire a developer",
  "Learn WordPress",
  "Write a blog post from a blank page",
  "Film yourself",
  "Think of content ideas",
  "Post daily to stay visible",
  "Pay monthly for a site builder",
];

const bySunday = [
  "A website on a domain you own, deploying automatically",
  "A pillar article and the start of a cluster around it",
  "Email capture that works — tested by you, not assumed",
  "A free resource with automated delivery",
  "Every reader routed to a free offer and a paid one",
  "Bridge pages, so every outbound link is yours to change",
  "A traffic source that compounds instead of resetting",
  "One article turning into a month of assets",
  "Brand-consistent images without your face in any of them",
  "A cadence you can hold at 2–4 hours a day",
];

const forYou = [
  "You run a Skool group and everything you've written lives inside it",
  "You sell digital products but don't own a real website",
  "You have content, courses or calls piling up with nowhere to send people",
  "You want a blog that compounds instead of a feed that resets",
  "You'd rather not be on camera",
];

const notForYou = [
  "You want someone to write your content for you",
  "You have no existing content and nothing to say yet",
  "You want a done-for-you agency build",
];

const faqs = [
  { q: "Do I need to know how to code?", a: "No, and you won't write a line of it. Claude generates every file; you paste it and click commit. If something is wrong the deploy fails and tells you in plain English, and you paste that back. The course assumes you have never opened a code editor." },
  { q: "How much is actually automated?", a: "The building and the writing. Claude produces the site files, the articles, the SEO passes, the lead magnets, the images and the twenty-four repurposed assets per article. What stays yours is approval — reading what came back and saying yes or saying what to change. That's deliberate. Anything published under your name should be read by you first." },
  { q: "What does it cost to run the site?", a: "About a dollar a month. GitHub and Vercel are free at your traffic level; the domain is $10–15 a year." },
  { q: "Do I need a paid Claude plan?", a: "For most of the course, no. But Module 1 uses Claude in Chrome \u2014 the browser extension that reads your platforms and builds your content inventory automatically \u2014 and that needs a paid Claude plan, the lowest tier is enough. If you'd rather not pay for it, Module 1 includes a full by-hand version that takes about an hour with a spreadsheet and gets you the same result. Nothing later in the weekend depends on which route you take. Beyond that, the only thing you have to pay for is your domain." },
  { q: "Can I really do it in a weekend?", a: "The build, yes — the modules are timed to it: two hours Friday, eight Saturday, five Sunday. Filling the site out afterwards is ongoing, which is what the operating cadence in Module 21 is for." },
  { q: "What if I don't use Skool?", a: "It works the same. Skool is the most common case because so much content gets trapped there, but the method applies to any pile of existing content — a course platform, a newsletter archive, a folder of PDFs." },
  { q: "Will I have to be on camera?", a: "No. Module 18 covers producing brand-consistent images without your face in any of them." },
  { q: "What if I don't have a paid product yet?", a: "Build the site anyway. Every article routes to a free resource, you build the list, and the list tells you what to sell. That's a better sequence than guessing at a product first." },
  { q: "How fast do I get access?", a: "I open it manually for each buyer rather than running an automated system, so it's usually within the hour during waking hours \u2014 but I'm one person, so please allow a few hours if you buy late at night. You'll get an email the moment your access is live, telling you exactly how to get in. If it's been longer than that, email hello@thedigitalincomeedit.com and I'll sort it immediately." },
  { q: "What happens when the tools change?", a: "You get the update, free. This is a live site you log into rather than a file you download, so when something changes — or I find a better way to do a step — the module is rewritten and new ones get added. Everyone who has ever bought sees the new version next time they log in. One payment covers all of it." },
  { q: "Is this part of your membership?", a: "No. It's a standalone one-time purchase and isn't part of any membership tier." },
];
---
<PageLayout
  ogImage="/og/weekend-ecosystem.jpg"
  wide
  title="The Weekend Ecosystem™"
  description="The website-and-blog system that turns digital product sellers into digital business owners. Built from what you already have, in one weekend, with Claude AI. $97 one-time."
>

  <!-- HERO -->
  <section class="we-hero">
    <div class="page-eyebrow">{S.eyebrow}</div>
    <h1>The Weekend <em>Ecosystem™</em></h1>
    <p class="lede">Your website + blog, built from what you already have. One weekend with Claude, and you're a business owner — not just a product seller.</p>
    <p class="we-frame">From the founder who built this entire business faceless, with AI — this is that exact build, packaged into one weekend.</p>
    <div class="we-cta-row">
      <a class="btn" href={HREF}>{S.cta} <span class="a">→</span></a>
    </div>
    <p class="we-access">{S.access}</p>
    <figure class="we-hero-img">
      <img src="/images/we/cover-sofa.jpg" alt="A woman working on a laptop on a sofa, beside the words: The Weekend Ecosystem — your website and blog, built from what you already have." loading="eager" width="1200" height="1490" />
    </figure>

    <div class="we-hero-strip">
      {phases.map((p, i) => (
        <span class="we-chip" style={`--ac:${p.accent}`}>
          <span class="we-chip-n">{String(i).padStart(2, "0")}</span>{p.title}
        </span>
      ))}
    </div>
  </section>

  <figure class="we-fig we-fig-desktop we-fig-tight">
    <img src="/images/we/the-numbers.jpg" alt="22 modules. 36 prompts, verbatim. 3 reference vaults. 1 weekend. Your website and blog, built from what you already have." loading="lazy" width="1200" height="1490" />
  </figure>
  <section class="we-stats we-mobile-only">
    {stats.map((s, i) => (
      <div class="we-stat" style={`--ac:${ACC[i * 2 > 5 ? 5 : i * 2]}`}>
        <span class="we-stat-n">{s.n}</span>
        <span class="we-stat-l">{s.l}</span>
      </div>
    ))}
  </section>

  <!-- THESIS -->
  <section class="we-block we-thesis">
    <h2>You already wrote it</h2>
    <p class="we-lead">Most people start a blog by opening a blank page and trying to think of something to say. Then they do it again Tuesday. Then they stop.</p>
    <p class="we-lead">You have the opposite problem — years of writing sitting in a classroom nobody can search, on a platform you don't own. Lessons. Call recordings. Posts. The DM where you explained your entire method to one person, for free, at midnight.</p>
    <p class="we-lead we-lead-hit">This weekend you don't write anything new. You build the place it all lives, works, and compounds.</p>
    <figure class="we-fig we-fig-wide">
      <img src="/images/we/hook-blog-posts.jpg" alt="You already wrote the blog posts. They're just sitting in your Skool group." loading="lazy" width="1200" height="1490" />
    </figure>
  </section>

  <!-- PROOF -->
  <section class="we-block we-proof">
    <span class="we-proof-tag">Built with this system</span>
    <h2>You're reading this on it</h2>
    <p class="we-sublead">This website, and every article on it, came out of the process in this course. Most of them started life as a lesson inside my Skool group. Open one and see what a Skool lesson looks like after a Saturday afternoon.</p>
    <div class="we-proof-grid">
      {proof.map((p) => (
        <a class="we-proof-card" href={p.u}>
          <span class="we-proof-n">{p.n}</span>
          <span class="we-proof-t">{p.t}</span>
          <span class="we-proof-go">Read it →</span>
        </a>
      ))}
    </div>
  </section>

  <!-- PROBLEM -->
  <section class="we-block">
    <h2>What's actually wrong right now</h2>
    <div class="we-grid">
      {problems.map((x, i) => (
        <div class="we-card">
          <span class="we-num">{String(i + 1).padStart(2, "0")}</span>
          <h3>{x.t}</h3>
          <p>{x.p}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- THE WEEKEND -->
  <section class="we-block">
    <h2>The weekend, hour by hour</h2>
    <p class="we-sublead">Six phases. Each builds a working piece, and each piece feeds the next — the website holds the blog, the blog fills the list, the list buys the product.</p>
    <figure class="we-fig we-fig-timeline">
      <img src="/images/we/weekend-timeline.jpg" alt="One weekend, hour by hour. 00 Friday night — Prep, 2 hrs. 01 Saturday morning — The Website, 3 hrs. 02 Saturday afternoon — The Blog, 3 hrs. 03 Saturday night — The Capture System, 3 hrs. 04 Sunday morning — Traffic, 3 hrs. 05 Sunday afternoon — Running It, 2 hrs." loading="lazy" width="1200" height="1490" />
    </figure>
    <div class="we-phases">
      {phases.map((p, i) => (
        <div class="we-phase" style={`--ac:${p.accent}`}>
          <span class="we-phase-edge"></span>
          <div class="we-phase-head">
            <span class="we-phase-n">{String(i).padStart(2, "0")}</span>
            <div>
              <span class="we-phase-when">{p.day}</span>
              <h3 class="we-phase-t">{p.title}</h3>
              <p class="we-phase-s">{p.sub}</p>
            </div>
          </div>
          <ul class="we-phase-mods">
            {p.mods.map((m) => (
              <li><span class="we-pm-n">{String(m.num).padStart(2, "0")}</span>{m.title}</li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </section>

  <!-- HANDS OFF -->
  <section class="we-block">
    <h2>Who does what</h2>
    <p class="we-sublead">This is the part people don't believe until they see the split. You are not building a website this weekend. You are approving one.</p>

    <figure class="we-fig we-fig-desktop">
      <img src="/images/we/who-does-what.jpg" alt="Who actually does the work. Claude does: writes every file your website needs, turns a lesson into a 3,000-word article, runs four SEO passes, builds the supporting article cluster, builds your email capture pages, writes your lead magnets, turns one article into twenty-four assets, generates every image, commits the files to your site. You do: paste the prompt, read what came back, say yes or say what to change. That's the whole job. Three moves, twenty-two times." loading="lazy" width="1200" height="1490" />
    </figure>

    <div class="we-who we-mobile-only">
      <div class="we-who-col we-who-ai">
        <span class="we-who-tag">Claude does</span>
        <ul>{claudeDoes.map((x) => <li>{x}</li>)}</ul>
      </div>
      <div class="we-who-col we-who-you">
        <span class="we-who-tag">You do</span>
        <ul>{youDo.map((x) => <li>{x}</li>)}</ul>
        <p class="we-who-note">That's the whole job. Three moves, twenty-two times.</p>
      </div>
    </div>

    <div class="we-never">
      <span class="we-never-tag">You never have to</span>
      <div class="we-never-grid">
        {neverDo.map((x) => <span class="we-never-chip">{x}</span>)}
      </div>
    </div>
  </section>

  <!-- WHAT'S INCLUDED -->
  <section class="we-block">
    <h2>What comes with it</h2>
    <div class="we-grid we-grid-3">
      {included.map((x, i) => (
        <div class="we-card we-card-pink" style={`--ac:${ACC[i]}`}>
          <h3>{x.t}</h3>
          <p>{x.p}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- INSIDE -->
  <section class="we-block">
    <h2>Inside the course</h2>
    <p class="we-sublead">Every prompt sits in a copy card. Every module has its own colour, a numbered spine of actions, and a collapsed panel of fixes for when something breaks.</p>
    <p class="we-sublead" style="margin-top:-24px"><a href="/weekend-ecosystem/preview" style="color:var(--pink);font-weight:500">Take a full look inside →</a> — the real interface, no email required.</p>
    <div class="we-inside">
      <div class="we-shot">
        <div class="we-shot-bar"><span></span><span></span><span></span></div>
        <div class="we-shot-body">
          <span class="we-shot-eyebrow">Phase 0 — Module 2 of 22</span>
          <span class="we-shot-h">Build Your Canon File</span>
          <div class="we-shot-step"><span class="we-shot-num">01</span><span class="we-shot-label">Copy and fill</span></div>
          <div class="we-shot-prompt">
            <div class="we-shot-prompt-bar"><span>Copy this prompt</span><em>Copy</em></div>
            <code>CANON FILE — [YOUR BRAND]<br/><br/>PRODUCTS — exact names, prices, URLs<br/>| Product | Price | Platform | URL |<br/><br/>DOES NOT EXIST — never reference<br/>- No free trial<br/>- No annual plan</code>
          </div>
          <div class="we-shot-road"><span>If you get stuck</span><em>+ Claude keeps forgetting my Canon File</em></div>
        </div>
      </div>
      <div class="we-inside-side">
        <figure class="we-fig we-fig-flush">
          <img src="/images/we/module-list.jpg" alt="The full list of 22 modules across six phases." loading="lazy" width="1100" height="1191" />
        </figure>
        <div class="we-mini">
          <span class="we-mini-tag">Progress</span>
          <div class="we-mini-ring"><span>22</span></div>
          <p>Tick each module off. The ring fills as you go, and every phase keeps its own tally.</p>
        </div>
        <div class="we-mini we-mini-cert">
          <span class="we-mini-tag">At twenty-two</span>
          <span class="we-mini-cert-h">Certificate of Completion</span>
          <p>Your name, the date, and something to post when you're done.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- REPURPOSING PROOF -->
  <section class="we-block">
    <h2>What one prompt actually produces</h2>
    <p class="we-sublead">This is the module people message me about afterwards. You publish an article, paste one prompt, and twenty-four finished assets come back. Below is real output from running it against this page.</p>

    <div class="rp-grid">
      <div class="rp-card">
        <div class="rp-tag">Three of the ten pins</div>
        <div class="rp-o"><b>How to turn your Skool content into a blog</b><i>Your best lessons are already written — they're just trapped where nobody can search them.</i></div>
        <div class="rp-o"><b>Where to find blog content you already wrote</b><i>Six places your best writing is already sitting. Most creators find dozens they'd forgotten.</i></div>
        <div class="rp-o"><b>You don't need to code to build a website</b><i>Everything that used to be required, and isn't anymore.</i></div>
      </div>

      <div class="rp-card">
        <div class="rp-tag">Three of the eight Threads posts</div>
        <div class="rp-q">"Count the lessons in your Skool classroom. Now count how many a stranger could find on Google today. That gap is the whole problem."</div>
        <div class="rp-q">"A post dies in three days. An article works for eighteen months. We spend almost all our time on the first one."</div>
        <div class="rp-q">"Renting: Skool, Instagram, TikTok, Pinterest. Owning: a domain, a site, an email list. Do both. But please do the second one."</div>
      </div>
    </div>

    <p class="rp-sum">Plus an 8-slide carousel, two reel prompts with matching cover stills, a Facebook post, an email and a community post. <strong>Twenty-four things across six platforms, from one afternoon</strong> — and the article underneath keeps ranking the whole time they circulate.</p>

    <p class="we-sublead" style="margin-bottom:0"><a href="/weekend-ecosystem/preview" style="color:var(--pink);font-weight:500">See the rest of the output →</a></p>
  </section>

  <!-- BY SUNDAY -->
  <section class="we-block">
    <h2>By Sunday afternoon</h2>
    <figure class="we-fig we-fig-desktop">
      <img src="/images/we/friday-sunday.jpg" alt="Friday: content scattered across a platform you don't own, no website, no blog, nobody finds you unless they already know you. Sunday: a site on a domain you own, a blog Google can find, an email list that captures, a system that runs without you. You didn't write anything new." loading="lazy" width="1200" height="1490" />
    </figure>
    <ul class="we-checks we-mobile-only">{bySunday.map((x) => <li><span class="we-tick"></span>{x}</li>)}</ul>
  </section>

  <!-- FIT -->
  <section class="we-block we-split">
    <div>
      <h3 class="we-sub">This is for you if</h3>
      <ul class="we-list we-list-tight">{forYou.map((x) => <li>{x}</li>)}</ul>
    </div>
    <div>
      <h3 class="we-sub">It isn't if</h3>
      <ul class="we-list we-list-tight we-list-muted">{notForYou.map((x) => <li>{x}</li>)}</ul>
    </div>
  </section>

  <!-- FAQ -->
  <section class="we-block">
    <h2>Questions</h2>
    <div class="we-faq">
      {faqs.map((x) => (
        <details>
          <summary>{x.q}</summary>
          <p>{x.a}</p>
        </details>
      ))}
    </div>
  </section>

  <!-- CLOSE -->
  <section class="we-close">
    <span class="we-close-eyebrow">One weekend from here</span>
    <h2>You were a digital product seller.</h2>
    <p class="lede">Now you own the infrastructure.</p>
    <div class="we-cta-row">
      <a class="btn" href={HREF}>{S.cta} <span class="a">→</span></a>
    </div>
    <p class="we-close-note">{S.note}</p>
    <p class="we-close-anchor">A developer quotes four figures for this build. A site builder charges more than this every year, forever. You pay once and own the thing.</p>
  </section>

  <style>
    .we-frame{font-size:15px;line-height:1.7;color:var(--taupe-deep);max-width:56ch;margin:-18px auto 30px}

    /* ── hero ── */
    .we-hero{text-align:center;padding:10px 0 54px}
    .we-hero .page-eyebrow{color:var(--pink)!important}
    .we-hero h1{font-family:"Fraunces",serif;font-weight:300;font-size:clamp(44px,8.4vw,86px);line-height:.96;letter-spacing:-2px;margin:0 0 26px;color:#2B161B}
    .we-hero h1 em{font-style:italic;color:var(--pink)}
    .we-hero .lede{max-width:600px;margin:0 auto 36px;font-family:"Cormorant Garamond",serif;font-style:italic;font-size:25px;line-height:1.5;color:#5A4048}
    .we-cta-row{display:flex;justify-content:center;gap:18px;flex-wrap:wrap}
    .we-soon{display:inline-block;padding:16px 36px;border-radius:4px;font-size:12px;letter-spacing:2.2px;text-transform:uppercase;
             background:rgba(214,46,115,.08);border:1px solid rgba(214,46,115,.3);color:var(--pink)}
    .we-hero-strip{display:flex;justify-content:center;gap:8px;flex-wrap:wrap;margin-top:46px}
    .we-chip{display:inline-flex;align-items:center;gap:7px;font-size:11px;letter-spacing:1.6px;text-transform:uppercase;padding:8px 15px;border-radius:22px;
             background:color-mix(in srgb,var(--ac) 26%,transparent);color:#3B2228;border:1px solid color-mix(in srgb,var(--ac) 60%,transparent)}
    .we-chip-n{font-family:"JetBrains Mono",monospace;font-size:10px;opacity:.55}

    /* ── stat strip ── */
    .we-stats{display:grid;grid-template-columns:repeat(4,1fr);gap:1px;background:rgba(43,22,27,.12);
              border:1px solid rgba(43,22,27,.12);border-radius:6px;overflow:hidden;margin-bottom:8px}
    .we-stat{background:#FBF8F5;padding:26px 18px;text-align:center}
    .we-stat-n{display:block;font-family:"Fraunces",serif;font-size:clamp(34px,4.6vw,50px);font-weight:300;line-height:1;color:var(--ac);letter-spacing:-1.5px}
    .we-stat-l{display:block;font-size:11px;letter-spacing:2px;text-transform:uppercase;color:#7A5C63;margin-top:10px}

    /* ── blocks ── */
    .we-block{padding:76px 0;border-top:1px solid rgba(43,22,27,.1)}
    .we-block h2{font-family:"Fraunces",serif;font-weight:300;font-size:clamp(30px,5vw,50px);line-height:1.04;margin:0 0 22px;letter-spacing:-1px;color:#2B161B;text-align:center}
    .we-lead{font-size:18.5px;line-height:1.8;max-width:640px;margin:0 auto 20px;color:#3B2228;text-align:center}
    .we-lead-hit{font-family:"Cormorant Garamond",serif;font-size:25px;font-style:italic;line-height:1.55;color:var(--pink);
                 border-left:none;padding:22px 0 0;margin:30px auto 0;text-align:center;border-top:1px solid rgba(214,46,115,.3);max-width:560px}
    .we-sublead{font-size:17px;line-height:1.72;color:#5A4048;max-width:640px;margin:0 auto 40px;text-align:center}
    .we-thesis{border-top:none;padding-top:70px}

    /* ── problem cards ── */
    .we-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-top:36px}
    .we-grid-3{grid-template-columns:repeat(auto-fit,minmax(250px,1fr))}
    .we-card{padding:28px 28px 26px;border-radius:6px;background:#FFFDFB;border:1px solid rgba(43,22,27,.14)}
    .we-card-pink{background:linear-gradient(160deg,color-mix(in srgb,var(--ac) 20%,transparent),color-mix(in srgb,var(--ac) 5%,transparent));
                  border:1px solid color-mix(in srgb,var(--ac) 44%,transparent)}
    .we-card h3{font-family:"Fraunces",serif;font-weight:400;font-size:20px;margin:0 0 11px;line-height:1.22;color:#2B161B}
    .we-card p{font-size:15.5px;line-height:1.7;margin:0;color:#57404A}
    .we-num{display:block;font-family:"JetBrains Mono",monospace;font-size:12px;letter-spacing:2px;color:var(--pink);margin-bottom:14px}

    /* ── phases ── */
    .we-phases{display:grid;grid-template-columns:1fr 1fr;gap:16px}
    .we-phase{position:relative;overflow:hidden;border:1px solid color-mix(in srgb,var(--ac) 40%,transparent);border-radius:6px;padding:26px 28px 24px;
              background:linear-gradient(165deg,color-mix(in srgb,var(--ac) 22%,transparent),color-mix(in srgb,var(--ac) 5%,transparent))}
    .we-phase-edge{position:absolute;left:0;top:0;bottom:0;width:6px;background:var(--ac)}
    .we-phase-head{display:flex;gap:16px;align-items:flex-start;padding-bottom:16px;margin-bottom:14px;border-bottom:1px solid color-mix(in srgb,var(--ac) 55%,transparent)}
    .we-phase-n{font-family:"Fraunces",serif;font-size:54px;font-weight:300;line-height:.8;color:var(--ac);letter-spacing:-2.5px;flex-shrink:0}
    .we-phase-when{display:block;font-family:"JetBrains Mono",monospace;font-size:10px;letter-spacing:2px;text-transform:uppercase;color:#8A6B44;margin-bottom:5px}
    .we-phase-t{font-family:"Fraunces",serif;font-weight:400;font-size:23px;margin:0 0 4px;line-height:1.12;color:#2B161B}
    .we-phase-s{font-size:14.5px;line-height:1.5;color:#5A4048;margin:0}
    .we-phase-mods{list-style:none;padding:0;margin:0}
    .we-phase-mods li{font-size:15.5px;line-height:1.45;padding:7px 0;color:#2B161B}
    .we-pm-n{font-family:"JetBrains Mono",monospace;font-size:10.5px;letter-spacing:1px;color:color-mix(in srgb,var(--ac) 78%,#2B161B);margin-right:10px}

    /* ── imagery ── */
    .we-hero-img{margin:44px auto 0;max-width:560px;padding:0}
    .we-hero-img img{width:100%;height:auto;display:block;border-radius:8px;box-shadow:0 14px 44px rgba(43,22,27,.14)}
    .we-fig{margin:0;padding:0}
    .we-fig img{width:100%;height:auto;display:block;border-radius:8px;box-shadow:0 10px 34px rgba(43,22,27,.11)}
    .we-fig-wide{max-width:520px;margin:42px auto 0}
    .we-fig-desktop{max-width:600px;margin:36px auto 0}
    .we-fig-tight{max-width:560px;margin:0 auto 8px}
    .we-fig-timeline{max-width:600px;margin:36px auto 40px}
    .we-fig-modules{max-width:640px;margin:38px auto 0}
    .we-fig-flush{margin:0}
    .we-fig-flush img{box-shadow:0 6px 22px rgba(43,22,27,.1)}
    .we-mobile-only{display:none}

    /* ── proof ── */
    .we-proof{border-top:none;padding-top:0}
    .we-proof-tag{display:block;font-family:"JetBrains Mono",monospace;font-size:10.5px;letter-spacing:2.6px;text-transform:uppercase;color:var(--pink);margin-bottom:14px}
    .we-proof-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin-top:34px}
    .we-proof-card{display:flex;flex-direction:column;gap:9px;padding:22px 22px 20px;border-radius:6px;text-decoration:none;
                   background:linear-gradient(165deg,rgba(203,183,247,.2),rgba(214,46,115,.06));
                   border:1px solid rgba(185,162,232,.5);transition:transform .2s,border-color .2s}
    .we-proof-card:hover{transform:translateY(-2px);border-color:var(--pink)}
    .we-proof-n{font-family:"JetBrains Mono",monospace;font-size:10px;letter-spacing:1.8px;text-transform:uppercase;color:#6B4FA8}
    .we-proof-t{font-family:"Fraunces",serif;font-size:19px;line-height:1.24;color:#2B161B}
    .we-proof-go{font-size:12.5px;letter-spacing:1.4px;text-transform:uppercase;color:var(--pink);margin-top:auto;padding-top:6px}

    /* ── inside the course ── */
    .we-inside{display:grid;grid-template-columns:1.42fr 1fr;gap:18px;margin-top:36px;align-items:start}
    .we-shot{border-radius:8px;overflow:hidden;border:1px solid rgba(43,22,27,.16);background:#FBF8F5;box-shadow:0 10px 34px rgba(43,22,27,.09)}
    .we-shot-bar{display:flex;gap:6px;padding:11px 14px;background:rgba(43,22,27,.055);border-bottom:1px solid rgba(43,22,27,.09)}
    .we-shot-bar span{width:9px;height:9px;border-radius:50%;background:rgba(43,22,27,.16)}
    .we-shot-body{padding:24px 26px 26px}
    .we-shot-eyebrow{display:block;font-family:"JetBrains Mono",monospace;font-size:9.5px;letter-spacing:2.2px;text-transform:uppercase;color:#CD9CDD;margin-bottom:10px}
    .we-shot-h{display:block;font-family:"Fraunces",serif;font-size:27px;font-weight:300;letter-spacing:-.6px;color:#2B161B;margin-bottom:22px}
    .we-shot-step{display:flex;align-items:center;gap:13px;margin-bottom:14px}
    .we-shot-num{width:31px;height:31px;border-radius:50%;background:#CD9CDD;color:#fff;display:flex;align-items:center;justify-content:center;
                 font-family:"JetBrains Mono",monospace;font-size:11px;flex-shrink:0}
    .we-shot-label{font-family:"Fraunces",serif;font-size:19px;color:#2B161B}
    .we-shot-prompt{border-radius:5px;overflow:hidden;border:1px solid rgba(214,46,115,.22);background:#FFFDFB;margin-bottom:14px}
    .we-shot-prompt-bar{display:flex;justify-content:space-between;align-items:center;padding:9px 12px;background:rgba(255,138,194,.16);border-bottom:1px solid rgba(214,46,115,.14)}
    .we-shot-prompt-bar span{font-family:"JetBrains Mono",monospace;font-size:9px;letter-spacing:1.8px;text-transform:uppercase;color:var(--pink)}
    .we-shot-prompt-bar em{font-family:"JetBrains Mono",monospace;font-size:9px;letter-spacing:1.6px;text-transform:uppercase;font-style:normal;
                           color:var(--pink);border:1px solid rgba(214,46,115,.4);border-radius:3px;padding:3px 9px;background:#fff}
    .we-shot-prompt code{display:block;padding:15px 16px;font-family:"JetBrains Mono",monospace;font-size:11.5px;line-height:1.75;color:#4A2C34}
    .we-shot-road{display:flex;flex-direction:column;gap:9px;border:1px solid rgba(214,46,115,.18);background:rgba(255,138,194,.06);border-radius:5px;padding:14px 16px}
    .we-shot-road span{font-family:"JetBrains Mono",monospace;font-size:9px;letter-spacing:1.8px;text-transform:uppercase;color:var(--pink)}
    .we-shot-road em{font-style:normal;font-size:14px;color:#2B161B}

    .we-inside-side{display:flex;flex-direction:column;gap:14px}
    .we-mini{border-radius:8px;padding:24px 24px 22px;border:1px solid rgba(43,22,27,.14);background:#FFFDFB}
    .we-mini-tag{display:block;font-family:"JetBrains Mono",monospace;font-size:9.5px;letter-spacing:2.2px;text-transform:uppercase;color:#8A6B44;margin-bottom:16px}
    .we-mini p{font-size:14.5px;line-height:1.6;color:#57404A;margin:16px 0 0}
    .we-mini-ring{width:78px;height:78px;border-radius:50%;display:flex;align-items:center;justify-content:center;
                  background:conic-gradient(#D62E73 0 100%,rgba(43,22,27,.09) 0);position:relative}
    .we-mini-ring:after{content:"";position:absolute;inset:9px;border-radius:50%;background:#FFFDFB}
    .we-mini-ring span{position:relative;z-index:1;font-family:"Fraunces",serif;font-size:23px;font-weight:300;color:#2B161B}
    .we-mini-cert{background:linear-gradient(150deg,rgba(185,162,232,.28),rgba(200,123,166,.16))}
    .we-mini-cert-h{display:block;font-family:"Fraunces",serif;font-size:21px;font-weight:300;color:#2B161B;padding-bottom:12px;border-bottom:1px solid rgba(214,46,115,.3)}

    /* ── access + anchor ── */
    .we-access{font-size:13px;letter-spacing:.3px;color:#7A5C63;margin:18px 0 0}
    .we-close-anchor{font-size:14px;line-height:1.65;color:rgba(251,238,244,.82);max-width:52ch;margin:20px auto 0}

    /* ── who does what ── */
    .we-who{display:grid;grid-template-columns:1.55fr 1fr;gap:18px;margin-bottom:26px}
    .we-who-col{border-radius:6px;padding:26px 28px 24px}
    .we-who-ai{background:linear-gradient(165deg,rgba(203,183,247,.26),rgba(203,183,247,.07));border:1px solid rgba(185,162,232,.5)}
    .we-who-you{background:linear-gradient(165deg,rgba(214,46,115,.16),rgba(214,46,115,.04));border:1px solid rgba(214,46,115,.34)}
    .we-who-tag{display:block;font-family:"JetBrains Mono",monospace;font-size:10.5px;letter-spacing:2.4px;text-transform:uppercase;margin-bottom:18px}
    .we-who-ai .we-who-tag{color:#6B4FA8}
    .we-who-you .we-who-tag{color:var(--pink)}
    .we-who-col ul{list-style:none;padding:0;margin:0}
    .we-who-col li{position:relative;padding:10px 0 10px 26px;font-size:15.5px;line-height:1.55;color:#2B161B;border-top:1px solid rgba(43,22,27,.09)}
    .we-who-col li:first-child{border-top:none;padding-top:0}
    .we-who-col li:before{position:absolute;left:0;top:10px;font-family:"JetBrains Mono",monospace;font-size:13px}
    .we-who-col li:first-child:before{top:0}
    .we-who-ai li:before{content:"\2726";color:#8B6FC4}
    .we-who-you li{font-size:17.5px;font-family:"Fraunces",serif;padding-top:13px;padding-bottom:13px}
    .we-who-you li:first-child{padding-top:0}
    .we-who-you li:before{content:"\2192";color:var(--pink)}
    .we-who-note{font-family:"Cormorant Garamond",serif;font-style:italic;font-size:19px;line-height:1.5;color:var(--pink);margin:20px 0 0;padding-top:16px;border-top:1px solid rgba(214,46,115,.28)}

    .we-never{border:1px solid rgba(43,22,27,.14);border-radius:6px;padding:24px 26px;background:#FFFDFB}
    .we-never-tag{display:block;font-family:"JetBrains Mono",monospace;font-size:10.5px;letter-spacing:2.4px;text-transform:uppercase;color:#8A6B44;margin-bottom:16px}
    .we-never-grid{display:flex;flex-wrap:wrap;gap:9px}
    .we-never-chip{position:relative;font-size:15px;line-height:1.4;padding:9px 15px 9px 15px;border-radius:5px;
                   background:rgba(43,22,27,.045);color:#7A5C63;text-decoration:line-through;text-decoration-color:rgba(214,46,115,.55);text-decoration-thickness:1.5px}

    /* ── repurposing proof ── */
    .rp-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-top:34px}
    .rp-card{border:1px solid rgba(43,22,27,.13);border-radius:7px;padding:22px 24px 20px;background:#FFFDFB}
    .rp-tag{font-family:"Inter",sans-serif;font-size:10px;letter-spacing:2.2px;text-transform:uppercase;color:var(--pink);margin-bottom:16px}
    .rp-o{padding:12px 0;border-top:1px solid rgba(43,22,27,.09)}
    .rp-o:first-of-type{border-top:none;padding-top:0}
    .rp-o b{display:block;font-family:"Fraunces",serif;font-weight:400;font-size:17.5px;color:#2B161B;margin-bottom:5px;line-height:1.25}
    .rp-o i{display:block;font-style:normal;font-size:14.5px;line-height:1.6;color:rgba(43,22,27,.66)}
    .rp-q{font-family:"Cormorant Garamond",serif;font-style:italic;font-size:17.5px;line-height:1.55;color:#3B2228;padding:12px 0;border-top:1px solid rgba(43,22,27,.09)}
    .rp-q:first-of-type{border-top:none;padding-top:0}
    .rp-sum{font-size:16.5px;line-height:1.75;color:#3B2228;max-width:660px;margin:26px 0 22px}
    .rp-sum strong{color:var(--pink);font-weight:600}
    @media(max-width:820px){.rp-grid{grid-template-columns:1fr}}

    /* ── ticked outcomes ── */
    .we-checks{list-style:none;padding:0;margin:36px 0 0;display:grid;grid-template-columns:1fr 1fr;gap:2px 30px}
    .we-checks li{position:relative;padding:14px 0 14px 32px;border-top:1px solid rgba(43,22,27,.1);font-size:16.5px;line-height:1.55;color:#2B161B}
    .we-tick{position:absolute;left:0;top:17px;width:18px;height:18px;border-radius:5px;background:var(--pink);display:flex;align-items:center;justify-content:center}
    .we-tick:after{content:"";width:5px;height:9px;border:solid #fff;border-width:0 2px 2px 0;transform:rotate(45deg) translate(-1px,-1px)}

    /* ── fit ── */
    .we-split{display:grid;grid-template-columns:1fr 1fr;gap:44px}
    .we-sub{font-family:"Fraunces",serif;font-weight:400;font-size:23px;margin:0;color:#2B161B}
    .we-list{list-style:none;padding:0;margin:18px 0 0}
    .we-list li{padding:12px 0 12px 28px;border-bottom:1px solid rgba(43,22,27,.1);font-size:15.5px;line-height:1.6;position:relative;color:#2B161B}
    .we-list li:before{content:"—";position:absolute;left:0;color:var(--pink)}
    .we-list-muted li{color:#7A5C63}
    .we-list-muted li:before{color:#B79AA3}

    /* ── faq ── */
    .we-faq{margin-top:32px;max-width:740px}
    .we-faq details{border-bottom:1px solid rgba(43,22,27,.12);padding:19px 0}
    .we-faq summary{cursor:pointer;font-size:17.5px;font-weight:500;list-style:none;color:#2B161B}
    .we-faq summary::-webkit-details-marker{display:none}
    .we-faq summary:before{content:"+";color:var(--pink);margin-right:13px;font-weight:400}
    .we-faq details[open] summary:before{content:"–"}
    .we-faq p{font-size:16.5px;line-height:1.78;margin:14px 0 4px 26px;color:#57404A;max-width:630px}

    /* ── close: the one saturated moment ── */
    .we-close{text-align:center;margin:80px 0 0;padding:80px 40px 74px;border-radius:8px;
              background:linear-gradient(145deg,#B9A2E8 0%,#C79AD2 34%,#C87BA6 64%,#A8446B 100%)}
    .we-close-eyebrow{display:block;font-family:"JetBrains Mono",monospace;font-size:10px;letter-spacing:3px;text-transform:uppercase;color:rgba(255,255,255,.7);margin-bottom:22px}
    .we-close h2{font-family:"Fraunces",serif;font-weight:300;font-size:clamp(30px,5.2vw,52px);margin:0 0 12px;letter-spacing:-1px;color:#fff}
    .we-close .lede{font-family:"Cormorant Garamond",serif;font-style:italic;font-size:28px;color:#FBEEF4;margin:0 0 36px}
    .we-close .btn{background:#FBF8F5;color:#8E2F53;border-color:#FBF8F5}
    .we-close .we-soon{background:rgba(251,248,245,.14);border-color:rgba(251,248,245,.45);color:#FBEEF4}
    .we-close-note{font-size:12.5px;letter-spacing:.6px;color:rgba(251,238,244,.72);margin-top:22px}

    @media(max-width:820px){
      .we-fig-tight{display:none}
      .we-mobile-only{display:grid}
      ul.we-mobile-only{display:block}
      .we-fig-desktop{max-width:100%;margin-top:28px}
      .we-who.we-mobile-only,.we-checks.we-mobile-only{display:none}
      .we-hero-img{max-width:100%;margin-top:34px}
      .we-fig-wide{max-width:100%}
      .we-stats{grid-template-columns:1fr 1fr}
      .we-grid,.we-phases,.we-checks,.we-split,.we-who,.we-inside{grid-template-columns:1fr}
      .we-proof-grid{grid-template-columns:1fr}
      .we-split{gap:34px}
      .we-block{padding:56px 0}
      .we-close{padding:60px 24px 54px;margin-top:60px}
    }
  </style>

  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "Product",
        "name": "The Weekend Ecosystem\u2122",
        "description": "A 22-module course that turns content you have already made into a website and blog on a domain you own, over one weekend, with Claude AI doing the building.",
        "brand": { "@type": "Brand", "name": "The Digital Income Edit\u2122" },
        "url": "https://www.thedigitalincomeedit.com/shop/weekend-ecosystem",
        "image": "https://www.thedigitalincomeedit.com/images/we/cover-sofa.jpg",
        "offers": {
          "@type": "Offer",
          "price": "97.00",
          "priceCurrency": "USD",
          "availability": "https://schema.org/InStock",
          "url": "https://www.thedigitalincomeedit.com/shop/weekend-ecosystem"
        }
      },
      {
        "@type": "FAQPage",
        "mainEntity": [
          { "@type": "Question", "name": "Do I need to know how to code?",
            "acceptedAnswer": { "@type": "Answer", "text": "No. Claude generates every file. You paste the prompt, read what comes back, and approve it. When something breaks, the site explains it in plain English and you paste that back." } },
          { "@type": "Question", "name": "Do I have to be a teacher or a coach?",
            "acceptedAnswer": { "@type": "Answer", "text": "No. You need content, not authority. Skool lessons, community posts, call recordings, PDFs and products all work as source material." } },
          { "@type": "Question", "name": "What if I do not have a free weekend?",
            "acceptedAnswer": { "@type": "Answer", "text": "It is sixteen hours split into six chunks. You can spread it across three weeks if that suits you better." } },
          { "@type": "Question", "name": "What if I do not have enough content?",
            "acceptedAnswer": { "@type": "Answer", "text": "Most people find forty to eighty pieces they had forgotten they made." } },
          { "@type": "Question", "name": "Is this a one-time payment?",
            "acceptedAnswer": { "@type": "Answer", "text": "Yes. One payment, and every future update is included permanently. It is delivered as a live website you log into, not a PDF you download, so modules are rewritten and added as tools change." } }
        ]
      }
    ]
  })} />
</PageLayout>
